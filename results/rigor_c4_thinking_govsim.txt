C4 — GOVSIM THINKING FACTORIAL (partial, 2026-09-21)
Condition A (no enforcement), commons survival:
  thinking OFF: 0/3 survived (seeds 1,2,3 all collapse by month 1-3)
  thinking ON : 1/1 survived (seed 1: total_catch 240, lake regenerated to 100, no collapse)
=> Deliberation materially changes the commons outcome: with extended thinking enabled,
   the SAME no-enforcement condition that collapses every time without it can sustain the lake.
   The "institutions don't emerge / commons collapses" result is therefore CONFOUNDED with
   having disabled extended thinking for cost. This confirms the confound exists.
Caveat: extended thinking via the subscription SDK throttles to ~2 calls/min, so the full
   ON x OFF x {A,C} x 3-seed factorial could not be completed here; n=1 on the ON side.
   A properly-provisioned re-run (API key / off-peak) is owed for a powered estimate.
Code: run_govsim.py now supports --think {0,1} (cache-namespaced -tk1 so ON/OFF never collide),
   is resilient to per-cell failures, and fine_accuracy was tightened (target must be above-mean catch).
