# Program v2 work queue. The runner (tools/run_queue.py) executes jobs in priority order;
# Phase B (the answer to Q0) is priority 1, Phase A priority 2 (plan v2.1 §2).
# Agents add a job here only after its experiment module exists in experiments/v2/, passes the
# offline tests, and (for full runs) after the pre-registration commit (plan §6).
# models: all  -> every model in the frozen roster.yaml (claude_sdk only with --include-claude)
jobs: []
